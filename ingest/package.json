{
  "name": "cribrum-ingest",
  "version": "0.1.0",
  "private": true,
  "description": "Spec ingestion scripts for Cribrum. Pulls HTML element model from WHATWG / @webref/elements and emits Cribrum.Html.Model.idr.",
  "type": "module",
  "scripts": {
    "ingest": "tsx html-model.ts",
    "ingest:check": "tsx html-model.ts --check"
  },
  "devDependencies": {
    "@webref/elements": "^2.6.0",
    "tsx": "^4.20.0",
    "typescript": "^5.6.0"
  }
}
